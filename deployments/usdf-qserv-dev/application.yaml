---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: qserv-dev
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: qserv-dev
  sources:
    - repoURL: ghcr.io/lsst/charts
      chart: qserv
      targetRevision: 2026.8.3-rc1
      helm:
        valueFiles:
          - $values/deployments/usdf-qserv-dev/values.yaml
    - repoURL: https://github.com/lsst/qserv-deployments.git
      targetRevision: main
      ref: values
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
